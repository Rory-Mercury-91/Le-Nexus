name: Discord Release Notification

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag de la release (ex: v1.0.2)'
        required: true
        default: 'v1.0.2'

jobs:
  notify-discord:
    name: Notify Discord
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract Version
        id: version
        run: |
          # Utiliser le tag de l'événement release ou l'input manuel
          if [ "${{ github.event_name }}" == "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
          else
            VERSION="${{ github.event.inputs.tag_name }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "version_number=${VERSION#v}" >> $GITHUB_OUTPUT

      - name: Extract Changelog
        id: changelog
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          VERSION_NUM="${VERSION#v}"
          
          echo "🔍 Extraction du changelog pour la version: $VERSION_NUM"
          
          # Extraire la section du CHANGELOG pour cette version
          # Format attendu: ## [X.Y.Z] - YYYY-MM-DD
          awk -v ver="$VERSION_NUM" '
            /^## \[/ {
              # Extraire la version entre crochets
              if (match($0, /\[([0-9]+\.[0-9]+\.[0-9]+[^\]]*)\]/, arr)) {
                if (arr[1] == ver) {
                  started = 1
                  next
                } else if (started) {
                  exit
                }
              }
            }
            started && /^## \[/ { exit }
            started { print }
          ' CHANGELOG.md > changelog_extract.txt
          
          # Vérifier si le changelog a été extrait
          if [ ! -s changelog_extract.txt ]; then
            echo "⚠️ ATTENTION: Changelog vide pour $VERSION_NUM" >&2
            echo "📝 Nouvelles fonctionnalités et corrections de bugs." > changelog_extract.txt
          else
            echo "✅ Changelog extrait ($(wc -l < changelog_extract.txt) lignes)"
          fi
          
          # Limiter à 900 caractères pour laisser de la marge (Discord limite à 1024)
          CHANGELOG_SIZE=$(wc -c < changelog_extract.txt)
          echo "📏 Taille du changelog: $CHANGELOG_SIZE caractères"
          
          if [ $CHANGELOG_SIZE -gt 900 ]; then
            head -c 897 changelog_extract.txt > changelog_short.txt
            echo "..." >> changelog_short.txt
            echo "truncated=true" >> $GITHUB_OUTPUT
            echo "⚠️ Changelog tronqué à 900 caractères"
          else
            cp changelog_extract.txt changelog_short.txt
            echo "truncated=false" >> $GITHUB_OUTPUT
          fi

      - name: Extract Summary
        id: summary
        run: |
          echo "🔍 Extraction du résumé..."
          
          # Extraire les 3 premières catégories principales
          SUMMARY=$(awk '
            /^### / {
              if (count++ < 3) {
                gsub(/^### /, "");
                print "• " $0
              }
            }
          ' changelog_extract.txt)
          
          # Limiter à 400 caractères pour le summary
          if [ -n "$SUMMARY" ]; then
            SUMMARY=$(echo "$SUMMARY" | head -c 400)
            echo "✅ Résumé extrait: ${#SUMMARY} caractères"
          else
            SUMMARY="✨ Nouvelles fonctionnalités et améliorations"
            echo "⚠️ Résumé par défaut utilisé"
          fi
          
          # Sauvegarder dans un fichier
          echo "$SUMMARY" > summary.txt

      - name: Fetch Release Asset
        id: asset
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          VERSION_NUMBER="${{ steps.version.outputs.version_number }}"
          REPO="${{ github.repository }}"
          
          echo "🔍 Recherche des assets pour $VERSION..."
          
          RESPONSE=$(curl -s \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            "https://api.github.com/repos/$REPO/releases/tags/$VERSION")
          
          # Chercher l'installateur .exe
          ASSET_URL=$(echo "$RESPONSE" | jq -r '.assets[]? | select(.name | endswith(".exe")) | .browser_download_url' | head -n 1)
          ASSET_NAME=$(echo "$RESPONSE" | jq -r '.assets[]? | select(.name | endswith(".exe")) | .name' | head -n 1)
          
          # Fallback si l'asset n'existe pas encore
          if [ -z "$ASSET_URL" ] || [ "$ASSET_URL" = "null" ]; then
            echo "⚠️ Asset .exe non trouvé, utilisation du nom par défaut"
            ASSET_NAME="Nexus-$VERSION_NUMBER.exe"
            ASSET_URL="https://github.com/$REPO/releases/download/$VERSION/$ASSET_NAME"
            echo "asset_exists=false" >> $GITHUB_OUTPUT
          else
            echo "✅ Asset trouvé: $ASSET_NAME"
            echo "asset_exists=true" >> $GITHUB_OUTPUT
          fi
          
          echo "download_url=$ASSET_URL" >> $GITHUB_OUTPUT
          echo "asset_name=$ASSET_NAME" >> $GITHUB_OUTPUT

      - name: Prepare Discord Payload
        id: payload
        env:
          VERSION: ${{ steps.version.outputs.version }}
          VERSION_NUMBER: ${{ steps.version.outputs.version_number }}
          ASSET_URL: ${{ steps.asset.outputs.download_url }}
          ASSET_EXISTS: ${{ steps.asset.outputs.asset_exists }}
          TRUNCATED: ${{ steps.changelog.outputs.truncated }}
        run: |
          # Lire les fichiers
          CHANGELOG_RAW=$(cat changelog_short.txt)
          SUMMARY_RAW=$(cat summary.txt)
          
          # Vérifier que les fichiers ne sont pas vides
          if [ -z "$CHANGELOG_RAW" ] || [ "$CHANGELOG_RAW" = "" ]; then
            CHANGELOG_RAW="📝 Contenu disponible dans les prochaines releases."
          fi
          
          if [ -z "$SUMMARY_RAW" ] || [ "$SUMMARY_RAW" = "" ]; then
            SUMMARY_RAW="✨ Nouvelles fonctionnalités et améliorations"
          fi
          
          # Limiter la longueur des champs (Discord limite à 1024 caractères par field value)
          # On limite à 900 pour avoir de la marge
          
          # Summary : max 400 caractères
          if [ ${#SUMMARY_RAW} -gt 400 ]; then
            SUMMARY_RAW="${SUMMARY_RAW:0:397}..."
            echo "⚠️ Summary tronqué à 400 caractères"
          fi
          
          # Changelog : max 900 caractères
          if [ ${#CHANGELOG_RAW} -gt 900 ]; then
            CHANGELOG_RAW="${CHANGELOG_RAW:0:897}..."
            echo "⚠️ Changelog tronqué à 900 caractères"
          fi
          
          echo "📏 Tailles finales:"
          echo "   Summary: ${#SUMMARY_RAW} caractères"
          echo "   Changelog: ${#CHANGELOG_RAW} caractères"
          
          # Préparer le message de téléchargement
          if [ "$ASSET_EXISTS" = "true" ]; then
            DOWNLOAD_MSG="**Windows** : [💾 Télécharger l'installateur]($ASSET_URL)"
          else
            DOWNLOAD_MSG="**Windows** : ⏳ L'installateur sera disponible sous peu\n[📦 Voir la page de release](https://github.com/Rory-Mercury-91/Le-Nexus/releases/tag/$VERSION)"
          fi
          
          # Ajouter un lien vers le changelog complet si tronqué (mais limiter la longueur totale)
          if [ "$TRUNCATED" = "true" ]; then
            CHANGELOG_WITH_LINK="${CHANGELOG_RAW}\n\n_[📖 Voir le changelog complet](https://github.com/Rory-Mercury-91/Le-Nexus/blob/main/CHANGELOG.md)_"
            # S'assurer que le total ne dépasse pas 1024
            if [ ${#CHANGELOG_WITH_LINK} -gt 1024 ]; then
              # Tronquer le changelog pour laisser de la place au lien
              MAX_CHANGELOG=$((1024 - 100))
              CHANGELOG_RAW="${CHANGELOG_RAW:0:$MAX_CHANGELOG}..."
              CHANGELOG_WITH_LINK="${CHANGELOG_RAW}\n\n_[📖 Voir le changelog complet](https://github.com/Rory-Mercury-91/Le-Nexus/blob/main/CHANGELOG.md)_"
            else
              CHANGELOG_RAW="$CHANGELOG_WITH_LINK"
            fi
          fi
          
          # S'assurer qu'aucun champ n'est vide (Discord refuse les champs vides)
          if [ -z "$SUMMARY_RAW" ] || [ ${#SUMMARY_RAW} -lt 1 ]; then
            SUMMARY_RAW="✨ Nouvelles fonctionnalités et améliorations"
          fi
          
          if [ -z "$CHANGELOG_RAW" ] || [ ${#CHANGELOG_RAW} -lt 1 ]; then
            CHANGELOG_RAW="📝 Consultez le CHANGELOG complet sur GitHub pour tous les détails."
          fi
          
          if [ -z "$DOWNLOAD_MSG" ] || [ ${#DOWNLOAD_MSG} -lt 1 ]; then
            DOWNLOAD_MSG="**Windows** : Voir la page de release"
          fi
          
          echo "🔍 Vérification finale avant génération JSON:"
          echo "   Summary non vide: ${#SUMMARY_RAW} caractères"
          echo "   Changelog non vide: ${#CHANGELOG_RAW} caractères"
          echo "   Download non vide: ${#DOWNLOAD_MSG} caractères"
          
          # Construire le JSON avec jq (échapper correctement les caractères spéciaux)
          jq -n \
            --arg username "Nexus Release Bot" \
            --arg avatar "https://github.com/Rory-Mercury-91.png" \
            --arg version "$VERSION" \
            --arg version_num "$VERSION_NUMBER" \
            --arg summary "$SUMMARY_RAW" \
            --arg changelog "$CHANGELOG_RAW" \
            --arg download "$DOWNLOAD_MSG" \
            --arg timestamp "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)" \
            '{
              "username": $username,
              "avatar_url": $avatar,
              "embeds": [{
                "title": ("🚀 Nexus " + $version + " - Nouvelle Version Disponible !"),
                "description": "**Une mise à jour importante de Nexus est maintenant disponible.**",
                "color": 5814783,
                "fields": [
                  {
                    "name": "✨ Points Clés",
                    "value": (if ($summary | length) > 0 then $summary else "Nouvelles fonctionnalités" end),
                    "inline": false
                  },
                  {
                    "name": "📋 Changements",
                    "value": (if ($changelog | length) > 0 then $changelog else "Voir le CHANGELOG" end),
                    "inline": false
                  },
                  {
                    "name": "⬇️ Téléchargement",
                    "value": (if ($download | length) > 0 then $download else "Voir la release" end),
                    "inline": false
                  },
                  {
                    "name": "🔗 Liens",
                    "value": ("📦 [Release](https://github.com/Rory-Mercury-91/Le-Nexus/releases/tag/" + $version + ")\n🐛 [Issues](https://github.com/Rory-Mercury-91/Le-Nexus/issues)\n📖 [Docs](https://github.com/Rory-Mercury-91/Le-Nexus#readme)"),
                    "inline": false
                  }
                ],
                "footer": {
                  "text": ("Nexus v" + $version_num),
                  "icon_url": $avatar
                },
                "timestamp": $timestamp
              }]
            }' > discord_payload.json
          
          # Valider le JSON
          if ! jq empty discord_payload.json 2>/dev/null; then
            echo "❌ Erreur: Le JSON généré est invalide"
            cat discord_payload.json
            exit 1
          fi
          
          # Vérifier que les embeds ne sont pas vides
          EMBED_COUNT=$(jq '.embeds | length' discord_payload.json)
          if [ "$EMBED_COUNT" -eq 0 ]; then
            echo "❌ Erreur: Aucun embed dans le payload"
            exit 1
          fi
          
          # Vérifier que chaque field a une value non vide
          EMPTY_FIELDS=$(jq '.embeds[0].fields[] | select(.value == "" or .value == null) | .name' discord_payload.json)
          if [ -n "$EMPTY_FIELDS" ]; then
            echo "❌ Erreur: Champs vides détectés: $EMPTY_FIELDS"
            exit 1
          fi
          
          # Afficher un résumé du payload
          echo "✅ Payload Discord validé:"
          echo "   Embeds: $EMBED_COUNT"
          echo "   Fields: $(jq '.embeds[0].fields | length' discord_payload.json)"
          jq '.embeds[0].fields[] | "   - \(.name): \(.value | length) caractères"' -r discord_payload.json
          
          echo "✅ Payload Discord préparé et validé"
          
          # Envoyer à Discord immédiatement
          echo ""
          echo "📤 Envoi de la notification Discord..."
          
          # Vérifier que le webhook est défini
          if [ -z "${{ secrets.DISCORD_WEBHOOK_URL_NEXUS }}" ]; then
            echo "⚠️ DISCORD_WEBHOOK_URL_NEXUS n'est pas défini, notification ignorée"
            exit 0
          fi
          
          # Afficher le payload complet pour debugging
          echo "📋 Payload complet à envoyer:"
          cat discord_payload.json | jq .
          
          # Envoyer la requête
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "${{ secrets.DISCORD_WEBHOOK_URL_NEXUS }}" \
            -H "Content-Type: application/json" \
            -d @discord_payload.json 2>&1)
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          RESPONSE_BODY=$(echo "$RESPONSE" | head -n -1)
          
          echo ""
          echo "📡 Code de réponse HTTP: $HTTP_CODE"
          
          if [ "$HTTP_CODE" -eq 204 ] || [ "$HTTP_CODE" -eq 200 ]; then
            echo "✅ Notification Discord envoyée avec succès !"
          else
            echo "❌ Erreur lors de l'envoi à Discord (HTTP $HTTP_CODE)"
            echo "📋 Réponse de Discord:"
            echo "$RESPONSE_BODY"
            
            # Si c'est une erreur 400, afficher plus de détails
            if [ "$HTTP_CODE" -eq 400 ]; then
              echo ""
              echo "💡 Erreur 400 - Le payload est rejeté par Discord"
              echo "   Causes possibles:"
              echo "   - Un field value est vide ou trop long (max 1024 caractères)"
              echo "   - L'embed total dépasse 6000 caractères"
              echo "   - Caractères spéciaux mal échappés"
              echo "   - Structure JSON invalide pour Discord"
            fi
            
            exit 1
          fi
