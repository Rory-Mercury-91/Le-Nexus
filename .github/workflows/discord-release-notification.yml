name: Discord Release Notification

on:
  release:
    types: [published, created]
  workflow_run:
    workflows: ["Build and Release"]
    types:
      - completed
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag de la release (ex: v1.0.0)'
        required: true
        default: 'v1.0.0'

jobs:
  notify-discord:
    name: Notify Discord
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract Version
        id: version
        run: |
          # Utiliser le tag de l'événement release ou l'input manuel
          if [ "${{ github.event_name }}" == "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
          else
            VERSION="${{ github.event.inputs.tag_name }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "version_number=${VERSION#v}" >> $GITHUB_OUTPUT

      - name: Extract Changelog
        id: changelog
        run: |
          chmod +x .github/scripts/extract-changelog.sh
          .github/scripts/extract-changelog.sh "${{ steps.version.outputs.version }}"

      - name: Get Release Notes
        id: release_notes
        if: github.event_name == 'release'
        run: |
          # Utiliser les notes de release GitHub si disponibles
          if [ -n "${{ github.event.release.body }}" ]; then
            echo "${{ github.event.release.body }}" | head -c 1000 > release_notes.txt
          else
            echo "Aucune note de release disponible." > release_notes.txt
          fi

      - name: Send Discord Notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL_NEXUS }}
          VERSION: ${{ steps.version.outputs.version }}
          VERSION_NUMBER: ${{ steps.version.outputs.version_number }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          # Lire le changelog et le résumé depuis les fichiers
          CHANGELOG_RAW=$(cat changelog_short.txt)
          chmod +x .github/scripts/extract-summary.sh
          SUMMARY_RAW=$(.github/scripts/extract-summary.sh)
          
          # Construire le JSON avec jq pour échappement correct
          jq -n \
            --arg username "Nexus Release Bot" \
            --arg avatar "https://github.com/$REPO_OWNER.png" \
            --arg version "$VERSION" \
            --arg version_num "$VERSION_NUMBER" \
            --arg summary "$SUMMARY_RAW" \
            --arg changelog "$CHANGELOG_RAW" \
            --arg repo_owner "$REPO_OWNER" \
            --arg repo_name "$REPO_NAME" \
            --arg timestamp "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)" \
            '{
              "username": $username,
              "avatar_url": $avatar,
              "embeds": [{
                "title": ("🚀 Nexus " + $version + " - Nouvelle Version Disponible !"),
                "description": "**Une mise à jour importante de Nexus est maintenant disponible.**\n\n",
                "color": 5814783,
                "fields": [
                  {
                    "name": "✨ Points Clés de cette Version",
                    "value": $summary,
                    "inline": false
                  },
                  {
                    "name": "📝 Changements Complets",
                    "value": $changelog,
                    "inline": false
                  },
                  {
                    "name": "⬇️ Téléchargements",
                    "value": ("**Windows** : [💾 Télécharger l'installateur](https://github.com/" + $repo_owner + "/" + $repo_name + "/releases/download/" + $version + "/Nexus%20Setup%20" + $version_num + ".exe)"),
                    "inline": false
                  },
                  {
                    "name": "🔗 Liens Utiles",
                    "value": ("📦 [Page de la Release](https://github.com/" + $repo_owner + "/" + $repo_name + "/releases/tag/" + $version + ")\n🐛 [Signaler un Bug](https://github.com/" + $repo_owner + "/" + $repo_name + "/issues)"),
                    "inline": false
                  }
                ],
                "footer": {
                  "text": ("Nexus v" + $version_num + " • Production Ready • Merci de votre soutien !"),
                  "icon_url": $avatar
                },
                "timestamp": $timestamp
              }]
            }' > discord_payload.json
          
          # Envoyer le JSON à Discord
          curl -X POST "$DISCORD_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d @discord_payload.json
