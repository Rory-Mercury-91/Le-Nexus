/**
 * Fonctions helper pour les handlers des paramètres
 */

// Import des fonctions communes
const { getUserIdByName, getPaths } = require('../common-helpers');

module.exports = {
  getUserIdByName,
  getPaths
};
